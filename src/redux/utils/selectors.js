export const selectNotification = (state) => state.utils.notification;

export const selectMenuToggle = (state) => state.utils.menuOpen;

export const selectInviterToggle = (state) => state.utils.inviterOpen;
